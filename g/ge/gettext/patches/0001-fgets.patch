--- ./gettext-runtime/gnulib-lib/stdio.in.h.orig	2012-10-13 13:40:37.214064414 +0400
+++ ./gettext-runtime/gnulib-lib/stdio.in.h	2012-10-13 13:40:51.460731580 +0400
@@ -141,7 +141,6 @@
    so any use of gets warrants an unconditional warning.  Assume it is
    always declared, since it is required by C89.  */
 #undef gets
-_GL_WARN_ON_USE (gets, "gets is a security hole - use fgets instead");
 
 #if @GNULIB_FOPEN@
 # if @REPLACE_FOPEN@
--- ./gettext-tools/gnulib-lib/stdio.in.h.orig	2012-10-13 13:40:42.974064616 +0400
+++ ./gettext-tools/gnulib-lib/stdio.in.h	2012-10-13 13:40:51.464064914 +0400
@@ -141,7 +141,6 @@
    so any use of gets warrants an unconditional warning.  Assume it is
    always declared, since it is required by C89.  */
 #undef gets
-_GL_WARN_ON_USE (gets, "gets is a security hole - use fgets instead");
 
 #if @GNULIB_FOPEN@
 # if @REPLACE_FOPEN@
--- ./gettext-tools/libgettextpo/stdio.in.h.orig	2012-10-13 13:40:47.667398114 +0400
+++ ./gettext-tools/libgettextpo/stdio.in.h	2012-10-13 13:40:51.464064914 +0400
@@ -141,7 +141,6 @@
    so any use of gets warrants an unconditional warning.  Assume it is
    always declared, since it is required by C89.  */
 #undef gets
-_GL_WARN_ON_USE (gets, "gets is a security hole - use fgets instead");
 
 #if @GNULIB_FOPEN@
 # if @REPLACE_FOPEN@
